using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Blocks : MonoBehaviour
{
    //checks if D I E blocks are all near each other in the proper order
    //when one block is placed down
    //I block checks left for D block, right

    //put the blocks inside of a specific zone in order to make the check easier?
}
